<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hmg.dooraylgn.mapper.MemberMapper">	<!-- mapper 인터페이스가 위치하는 경로 -->
    <!-- id: mapper 인터페이스에서 선언한 메서드 이름과 같아야 한다. resultType: VO 클래스가 위치하는 경로 -->
    <!-- <select id="getUserList" resultType="<패키지 이름>.vo.UserVo">	
        SELECT *
        FROM User	
    </select> -->

    <!-- <mapper namespace="com.example.demo.sample.mapper.SampleMapper">
	<select id="selectTest" resultType="string">
		select 'test'
	</select>
    </mapper> -->
</mapper>